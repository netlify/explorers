{
  "private": true,
  "name": "@netlify/jamstack-explorers",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "build": "next build && next export",
    "develop": "next dev",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "prestart": "npm run build",
    "start": "next start",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  },
  "dependencies": {
    "next": "^9.5.2",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  }
}
